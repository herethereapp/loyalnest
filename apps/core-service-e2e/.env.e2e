# Server port for tests (avoid conflicts )
PORT=3102

# Use test database (a dedicated DB or schema)
DATABASE_URL=postgresql://test_user:test_pass@localhost:5432/core_service_test_db

# Use separate Redis or test namespace
REDIS_URL=redis://localhost:6379/1

# Kafka - either mock or a local Kafka instance for tests
KAFKA_BROKERS=localhost:9092

# JWT secret for testing (can be a fixed dummy string)
JWT_SECRET=test_jwt_secret

# Disable or mock MFA and external auth in tests if possible
MFA_PROVIDER=mock

# Logging level to verbose for debugging
LOG_LEVEL=debug

# Other feature flags for testing
FEATURE_GDPR=false

# Jest or test runner timeouts can be configured in jest.config.ts as well
